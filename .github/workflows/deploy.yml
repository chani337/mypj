name: Production CD Deploy

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Deploy to Remote Server
        run: echo "Deploying via SSH / Docker Compose Prod..."

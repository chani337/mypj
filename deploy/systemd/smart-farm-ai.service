[Unit]
Description=Smart Farm FastAPI AI Server
After=network.target

[Service]
User=smartfarm
WorkingDirectory=/opt/smartfarm/ai
ExecStart=/opt/smartfarm/ai/venv/bin/uvicorn app.main:app --host 0.0.0.0 --port 8000
Restart=always

[Install]
WantedBy=multi-user.target
